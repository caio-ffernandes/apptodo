import 'package:flutter/material.dart';
import 'element_screen.dart';

class WaterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ElementScreen(
      title: 'Tarefas da Água',
      color: Colors.blue,
    );
  }
}
