import 'package:flutter/material.dart';
import 'element_screen.dart';

class FireScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ElementScreen(
      title: 'Tarefas do Fogo',
      color: Colors.red,
    );
  }
}
